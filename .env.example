# Optional: override the app URL + dev-server port for local tunnel dev.
# Copy to `.env` and adjust. `shopify app dev` sets these for you.
# HOST=https://your-tunnel.example.com
# PORT=8080
SHOPIFY_API_SECRET_KEY=
